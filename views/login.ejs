<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Login - TodoPro</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="topbar"><div class="brand">TodoPro</div><div></div></div>
  <div class="container auth-hot">
    <div class="card auth-card">
      <h2>Welcome Back</h2>
      <% if (typeof error !== 'undefined') { %>
        <div class="small" style="color:var(--danger);margin-bottom:12px"><%= error %></div>
      <% } %>
      <form method="POST" action="/login">
        <input name="username" placeholder="Username" required />
        <input name="password" type="password" placeholder="Password" required />
        <button class="btn primary" type="submit">Login</button>
      </form>
      <p class="small" style="margin-top:14px">Don't have an account? <a href="/register">Register</a></p>
    </div>
  </div>
</body>
</html>